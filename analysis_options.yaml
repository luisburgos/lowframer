include: package:very_good_analysis/analysis_options.yaml

analyzer:
  exclude:
    # Both sub-packages have their own analysis_options.yaml and are analyzed
    # as their own packages by CI's sub_packages job.
    - "example/**"
    - "showcase/**"
